# Project Progress

Last updated: `2026-07-24`

`AI_HANDOFF.md` is the canonical continuation guide. This file records completion evidence, open verification, and the prioritized backlog.

## Current status

| Area | Status | Evidence / qualification |
|---|---|---|
| Web-admin header branding | Updated and locally verified | Replaced the visible `HACOM Admin` wordmark with `TrucTiepGAME` while retaining the existing Tailwind typography/color treatment, and aligned the app icon's accessible label. Both app typecheck/lint/build pipelines pass; backend unit tests pass 208/208 and integrations pass 28 with 14 guarded skips. |
| Product Frame admin visibility | Hidden and locally verified | Removed `Quản lý khung sản phẩm` from the sidebar and made the list, product-assignment, and edit routes terminate with Next.js `notFound()`. Dormant implementation code, permissions, and data remain unchanged. Both app typecheck/lint/build pipelines pass; backend unit tests pass 208/208 and integrations pass 28 with 14 guarded skips. |
| Voucher direct SKU/category scope | Implemented, migrated, and clone-verified | Voucher scope is now the deduplicated OR-union of up to 500 direct SKUs and dynamic category roots; both lists empty remains global. `web_admin_voucher_products` uses `(voucher_id,product_id)` primary ownership plus reverse product lookup and a cascading helper-table FK. Admin create/edit/list, product-detail discovery and checkout quote share the rule; storefront scope text is author-owned through the existing description. A restore-verified 310-table/100,214-row backup preceded live migration; 2 vouchers/31 category links remained unchanged and the fresh table had 0 rows. Focused unit passes 13/13, live schema integration 1/1 and destructive clone integration 2/2. Both app typechecks/lints/builds pass, backend unit passes 208/208, integrations pass 28 with 14 guarded skips, and healthcheck passes 22/22. |
| Shared news metadata and PC Build links | Implemented and browser-tested | The article header, all related-article cards, category list-card footers and all three first-row hero cards share `NewsCardMeta`: semantic publish date followed by localized view count at a measured 16px gap, sourced from `createDate`/`visit` instead of PCM author text. The shared PC Build banner uses `PC BUILD BỞI CHUYÊN GIA`, and both office/gaming cards intentionally open `/pc-van-phong.html` in protected new tabs. Focused article/category Playwright passes 13 applied desktop/mobile cases with 3 intentional skips, including exact metadata geometry and API binding. Both app typecheck/lint/build pipelines, 198 unit tests, 27 applied integrations with 13 guarded skips, and healthcheck 22/22 pass. |
| Managed product-promotion emphasis | Implemented and browser-tested | Plain managed promotion text on product detail is rendered in semantic `<strong>`, matching the bold editor-authored promotion rows while leaving an optional detail link outside the emphasis. The requested route confirms item 1 has one direct strong element at computed weight 700. Focused Playwright passes desktop/mobile including Axe and overflow; both app typecheck/lint/build pipelines, 198 unit tests, 27 applied integrations with 13 guarded skips, and healthcheck 22/22 pass. |
| Product-gallery utility/arrow layering | Implemented and browser-tested | The thumbnail rail uses an isolated local stacking context: arrows are layer 1 and specification/video utilities are positioned at layer 2. The utility therefore wins hit-testing at the real overlap without disabling arrow pointer events or changing geometry; arrows continue scrolling when exposed. Focused Playwright passes 3/3 desktop and the applicable mobile case, including modal opening and zero overflow. Both app typecheck/lint/build pipelines, 198 unit tests, 27 applied integrations with 13 guarded skips, and healthcheck 22/22 pass. |
| Product-detail Combo configurator | Implemented and browser-tested | Replaced the legacy picker cards with the canonical `ProductGridCard`, whose internal Combo mode shows Combo pricing and toggles selection without touching the ordinary cart. The tech-premium dialog keeps header/tabs/search/pagination fixed around a responsive 2/3/4/5/6-column scroll area, centers sparse desktop groups and supports keyboard tabs, focus restoration, loading/retry/empty states and protected new-tab product links. Focused Combo Playwright passes 5 applied cases/1 intentional matrix skip with Axe; shared-card regression passes 11/11. Both app typecheck/lint/build pipelines, 198 unit tests, 27 applied integrations with 13 guarded skips, and healthcheck 22/22 pass. |
| Desktop product-gallery sticky boundary | Implemented and browser-tested | From the existing 1200px three-column breakpoint, a shorter gallery sticks at 110px and travels only through the measured purchase-column height. `ResizeObserver` updates eligibility and the exact boundary when either column or the viewport changes; equal/taller galleries and sub-1200px layouts remain static. Focused Playwright passes 2/2 across 1200/1440/1920px, breakpoint and dynamic-height cases; related product-detail regression passes 4 applied/1 intentional skip. Both app typecheck/lint/build pipelines, 198 unit tests, 27 applied integrations with 13 guarded skips, and healthcheck 22/22 pass. |
| Product-detail Combo footer visibility | Implemented and browser-tested | `.product-bundle-footer` is absent until the first add-on SKU is selected, remains present through quote loading/result, and is removed when the last SKU is deleted or Combo selection resets. Quote/Cart/API/CSS contracts are unchanged; focused Playwright passes 2/2 across desktop/mobile Chromium. |
| Combo Set product/category scope | Implemented, migrated, and locally verified | The Combo product page now adds direct SKUs through the shared searchable picker and category roots through the existing two-column hierarchy selector. The effective list deduplicates direct and inherited products, labels sources and only removes direct relations from product rows. `web_admin_combo_set_categories` stores logical category roots; public summaries and quote validation dynamically include current descendants. Category import backup/rollback preserves the new scope. Migration applied to `it_tech_db` without changing Combo 713. Both app typecheck/lint/build pipelines pass, backend passes 198 unit tests and 27 applied integrations/13 guarded skips, focused write-enabled integration passes 1/1, and healthcheck passes 22/22. |
| Combo Set creation and editor controls | Implemented and locally verified | The list's `Thêm mới` link opens the no-ID editor and Combo Set POST/PATCH/DELETE permissions resolve to create/update/delete. New records default to the native-radio `Không giới hạn thời gian` mode and submit `toTime: 0`; bounded mode seeds a preserved +30-day draft and validates it against the start time. Product discounts are digit-only text controls, and newly created expanded groups are prepended above existing groups. Existing records hydrate by the zero/positive sentinel and the list labels zero as `Không giới hạn`. Both app typecheck/lint/build pipelines pass, backend passes 197 unit tests and 27 applied integrations with 12 guarded skips, focused Combo Set tests pass 12/12, and healthcheck passes 22/22. No API, schema, database or storefront runtime contract changed. |
| Brand editor and shared Brand/Collection layout | Implemented and browser-tested | Edit loads full Brand content on demand into an accessible 1200px modal. The plain `summary` is labeled `Mô tả tóm tắt`; seller-zero `description` is the offline TinyMCE `Mô tả`; ordering is a strict numeric text control. Logo selection previews locally and uploads only on save through a permission-scoped, signature-checked `MEDIA_ROOT/brand/...` route before transactional PATCH. Brand uses the shared Collection catalog layout, but its H1 is suppressed when sanitized editor content has under 10 readable characters and no safe banner image; Collection H1 remains unconditional. Both app typecheck/lint/build pipelines pass, backend passes 191 unit tests and 27 integrations with 12 guarded skips, focused Brand Playwright passes 5 desktop and 4 mobile cases with one intentional skip, and healthcheck passes 22/22. |
| Desktop product-description standalone images | Implemented and browser-tested | Product CMS HTML has a product-only hook. At 1024px and above, direct images and sole images inside direct paragraphs—including the live `p > span > img` shape—are centered with a 60% minimum width, 100% cap, automatic height and 20px vertical margins. Multi-image rows, fallback content and smaller viewports remain unchanged. Focused product/category Playwright passes 4 applied cases with 4 intentional skips; both app pipelines, 186/186 unit tests, 27 applied integrations with 11 guarded skips and healthcheck 22/22 pass. |
| Product-card attribute Preview | Implemented and locally verified | `/product/card-attributes` selects a real enabled positive-price SKU from the chosen category or its active descendants, preferring direct links. Parent categories with descendant-only inventory no longer render `No preview product`; duplicate draft/inherited rule keys and repeated legacy values collapse to one stable badge per attribute/value/slot. Focused preview tests pass 2/2; both app pipelines, 186/186 unit tests, 27 applied integrations with 11 guarded skips and healthcheck 22/22 pass. |
| Product Group two-column editor | Implemented and locally verified | Every attribute has left-side value editing and a right-side per-attribute SKU table. New attributes are inserted first and focused; each SKU select can create, reuse or fill a blank value through an Enter/Escape-capable inline form, updating the left list and SKU selection atomically while preserving the 50-value limit and unchanged API/database contracts. Focused helper coverage passes 5/5; both app typecheck/lint/build pipelines pass, backend unit tests pass 184/184, integrations pass 26 with 11 guarded skips, and healthcheck passes 22/22. |
| Admin voucher form/category selector | Implemented and locally verified | `/sales/vouchers` uses digit-only text controls with inline boundary validation and numeric payload conversion. Its category scope uses two desktop columns—selected items on the left and the searchable parent-child tree on the right—with native selection semantics, ancestor-path expansion, conflict removal and legacy selection chips. No API/schema/database change. Both app typecheck/lint/build pipelines pass, backend passes 179 unit and 26 applied integration tests with 11 guarded skips, and healthcheck passes 22/22. |
| Product-detail related grids 6 × 2 | Implemented and browser-tested | “Sản phẩm tương tự” and “Sản phẩm đã xem” share six initial / 12 maximum cards and a two/three/five/six-column responsive contract, with six columns beginning at 1536px. Recently viewed retains storage version 1, excludes the current product and bounds storage/revalidation to 12 prior cards. Focused Playwright passes 13/13 across count boundaries, keyboard disclosure, square media and the 390–1920px matrix; both app verification pipelines, 173 unit tests, 25 applied integrations with 12 guarded skips and local healthcheck 22/22 pass. |
| Final manual-component audit (2026-07-24) | Functionally verified; performance/load gates remain open | Audited the complete dirty bundle across storefront/admin permissions, uploads, validation, responsive behavior, accessibility, tests and dependency locks. Patched PostCSS to 8.5.22 (both production audits: 0 vulnerabilities), protected the external Zalo tab, corrected product-badge WCAG contrast, made carousel reduced-motion truly static and added isolated production-server controls for Playwright. Typecheck/lint/build pass in both apps; backend passes 197 unit and 27 applied integration tests with 12 guarded skips; healthcheck passes 22/22. Full four-worker Playwright executed 210 pass/79 intentional skips/5 parallel timing-state failures, and every failed case passed the immediate one-worker rerun (6 pass/4 project skips). All JS regression/release budgets and production-like 1,500-VU validation remain open. |
| Mobile search grid/filter drawer | Implemented and browser-tested | Below 1024px, `/tim` uses the category catalog's 12px inset, two-card grid, solid-blue filter trigger, left native dialog and shared max-five-token pagination. Sort, price and attributes reuse one desktop/mobile render helper with unique IDs; query changes preserve `q`, reset page one and keep the drawer open, while reset also preserves sort. Desktop remains unchanged. Focused Playwright passes 4/4 in each desktop/mobile Chromium project, shared category/catalog regression passes 24/24, both app verification pipelines pass, backend tests pass at 173 unit plus 25 applied integration/12 guarded skip, and both local health modes pass at 22/22. |
| Mobile product-category grid/filter drawer | Implemented and browser-tested | Below 1024px, the category's child links and product catalog render as two-column grids; the solid-blue toolbar trigger opens the shared-state left filter dialog and mobile sort now has one native arrow. Pagination is capped at five adaptive tokens on one row, while static category HTML aligns to the 12px product-grid inset below 640px without changing larger viewports. The drawer retains bottom-nav clearance, focus restoration and button/Escape/backdrop/desktop dismissal. Focused Playwright passes 5/5 in each desktop/mobile Chromium project across 390/768/1023/1024px with geometry, overlap/overflow, pagination start/middle/end, filter URL/reset and Axe coverage. Both app typecheck/lint/build pipelines, 173 unit tests, 25 applied integrations with 12 guarded skips, and both 22/22 local health modes pass. |
| Shared responsive storefront Footer | Implemented and focused-browser-tested | Rebuilt the shared Footer around the unchanged managed four-group/partner contracts and preserved all Vietnamese TrucTiepGAME/contact/legal/certification content. Desktop uses a centered 1800px composition, wider columns and a balanced 1–2px type increase from 1280px; height follows content and ends with the intentional 64px padding instead of a forced 1095px minimum. Mobile uses 24px insets, two-column links, one-column contacts and same-row certifications without horizontal overflow. The email field accepts input/autofill while submit stays unavailable, and the partner track centers when it fits. Storefront typecheck/lint and focused Playwright 4/4 including Axe pass. |
| Full workspace audit (2026-07-22) | Functionally verified; performance budgets still open | Reviewed all staged/manual component, API, PC Builder, migration, test, media and documentation changes. Fixed homepage link semantics, responsive geometry, stale-cache test races and long-matrix timing. Both app typechecks/lints/builds pass; backend unit 166/166 and integration 24 pass/12 guarded skip; the complete 232-case Playwright suite passes at 171 executed/61 intentional project skips/0 fail; healthcheck 22/22; both npm audits report zero vulnerabilities. JS regression/release budgets remain failing except combo-cart. |
| Managed Header utility links | Implemented, published, and browser-tested | Account, cart, favorites and assistant now come from web-admin `utilityLinks` in managed order on desktop/mobile. Public payload includes `systemKey` and visibility flags; account dropdown and cart count remain behavioral controls while assistant remains a button. Version 18 published with a protected-area hash match and ignored snapshot. Both app typechecks/lints/builds, 166 unit tests, 24 applied integrations with 12 guarded skips, focused Playwright 4/4 and healthcheck 22/22 pass; `style.css` SHA-256 remained unchanged. |
| PC Builder v6 | Implemented, migrated and verified | Optional per-SKU Build PC price, dynamic active-required/min-selection eligibility, direct-price precedence, campaign fallback, incomplete-build catalog/Flash pricing, candidate/quote/order revisions, storefront progress and price badges. Fresh 308-table/95,634-row backup restore-verified; retained clone passed apply twice/verify and live passed apply/verify with one logical-reference table, five columns, one index and no FK/orphans. Both app typechecks/lints/builds, 162 unit tests, 22 applied integrations, guarded clone regression, focused Playwright 16/16 desktop/mobile and isolated production healthcheck 22/22 pass. |
| PC Builder v5 | Implemented, migrated and browser-tested | Dense dark reference layout, quantity 1–4, deterministic SKU/category-descendant Build PC promotions, non-stacking Flash Sale comparison, optimistic/audited admin promotion tab, batch ordinary-cart handoff, direct promotion-aware checkout, real XLSX, branded PNG, print sheet, local AVIF/WebP artwork and six FAQ items. Fresh 305-table/95,634-row backup restore-verified; retained clone passed migration twice and live passed apply/verify (3 tables/3 FKs/4 indexes). Both app checks/builds, 161 unit tests, 21 applied integrations, focused Playwright 14/14 desktop/mobile (including exports) and healthcheck 22/22 pass; real CPU catalog/quantity quote smoke returns 112 candidates and quantity 4. |
| Quick tools / incomplete product attributes | Implemented in `web-admin`; destructive DB fixture remains disposable-only | Dark three-step category → attribute → SKU flow with a guarded root-first parent-child disclosure tree, stable admin sibling order, selected/search-path auto-expansion, local accent-insensitive category filtering, and URL-persistent filters. Mapping-aware active descendants, missing-only deduplicated SKU pagination, multi-value checkbox autosave, per-SKU coalescing, optimistic revision conflicts, audit metadata, and cross-consumer cache invalidation remain unchanged. No schema/index/public API change. Read benchmark: category summary ~236 ms cold / ~247 ms under `EXPLAIN ANALYZE`, attribute summary ~10 ms, first 20 missing SKU rows ~9 ms. Both app typechecks/lints/builds, 155 unit tests, 20 applied integration tests, and 19/19 health checks pass; browser write E2E remains gated on an authorized disposable admin environment. |
| PC Builder | Catalog-live v4 applied and enabled for local admin testing | All sellable category-tree SKUs are SQL-paginated without profile verification. Bidirectional attribute-value relations share a 90% per-side coverage gate so sparse catalog attributes cannot hide valid descendant SKUs; admin exposes the measured coverage and skipped state. Independent SSD/HDD, warning-bound checkout, catalog revisions, searchable admin taxonomy, and the dark responsive modal are implemented. Legacy browser drafts are server-normalized from `storage` to visible `ssd`/`hdd`, persistence waits for hydration, and signature-bound quote state rejects stale responses after reset/rapid changes. Restore-verified clone and live apply-twice/verify passed; historical build/order requote remains compatible. Both apps are running, focused desktop/mobile Playwright passes, local healthcheck passes 19/19, and `ADMIN_WRITE_ENABLED=true` is open only for testing. Auto/release remains 503; benchmark/policy data is retained. |
| Banner location deletion | Implemented, migrated, and browser-tested | Admin can permanently delete a non-default location after an accessible confirmation. Owned banners are transactionally moved to the protected `unassigned` location, synchronized, hidden, and retained with metadata/category/log relations; public APIs exclude the holding location. The guarded migration added a unique location-key index and singleton `Chưa có vị trí`; clone apply/idempotency/rollback/concurrency, live apply-twice/verify, Axe and focused Playwright pass. |
| Homepage circle-story carousel | Implemented and browser-tested | Section 2 keeps the managed `circleStory` payload and server-bootstrap fallback while matching the compact reference: every story uses a 3px gradient ring, a 3px dark spacer and artwork inset by 6px; outer geometry remains 73px rings/81px steps at 397px and fourteen centered 96px rings with 32px gaps inside a 1920px desktop rail. It no longer consumes legacy story CSS/IDs; the existing mobile-only controller provides autoplay, drag suppression, lifecycle cleanup and reduced-motion native scrolling. Focused Playwright passes exact inner/outer geometry, breakpoint transitions, links, overflow and Axe coverage. |
| Homepage hero carousel | Implemented and browser-tested | Section 3 keeps dynamic `slide_home` content while matching the responsive reference: 1856×381px inside a centered 1920px desktop rail and 369×143px at 401px using the separate mobile artwork. The legacy hero CSS is no longer consumed; focused Playwright verifies geometry, art direction, typography breakpoints, autoplay, swipe click suppression, keyboard control, pause lifecycle, reduced motion and Axe. |
| Homepage shop-by-category carousel | Implemented and browser-tested | Section 4 keeps its managed homepage-menu data and shared autoplay controller while matching the compact reference: a centered 1920px desktop rail with 130×110px cards/154px steps and a 398px mobile layout with 90×95px cards/100px steps. Focused Playwright verifies exact geometry, breakpoint density, Next/autoplay, lazy visible images, reduced motion, overflow, keyboard focus and WCAG A/AA serious/critical results. |
| Homepage featured deal cards | Implemented and browser-tested | Section 5 renders five local AVIF category links in a self-contained Server Component without the shared legacy card CSS. The 2545px reference uses a centered 1800px three-column/two-row grid with a full-height Pre-built card; the 428px reference uses the exact two-column asymmetric composition. Focused Playwright verifies both geometries, responsive transitions, routes, image loading, keyboard focus, hover treatment, overflow and WCAG A/AA serious/critical results. |
| Homepage category promotion cards | Implemented and browser-tested | Sections 9 and 14 are separate source components with independently owned card configuration, identifiers and utility markup; they initially render the same five local AVIF category links and geometry. Desktop from 1500px uses a centered five-card row capped at 1700px with 30px gaps and 5:6 cards; the 429px reference uses two 3:2 cards above three 25:23 cards. Focused Playwright compares both sections through all responsive breakpoints and verifies links, image loading, focus, overflow, reduced motion and WCAG A/AA serious/critical results. |
| Homepage brand gallery | Implemented and browser-tested | Section 15 renders the dynamic bootstrap brand set in a self-contained 2/3/4/5/6-column gallery with 80px cards, responsive 366/378px internal scrolling, fade/chevron and in-place expand/collapse controls. Desktop uses a centered 1800px outer wrapper with the existing 32px side padding, a 16px-radius inner shell and a 24px-radius glass frame inset by 16px; the inner frame stays hidden on mobile. Canonical links, logo/text fallback, component-scoped glow/scrollbar treatment, keyboard focus and reduced motion remain intact without changing the public brand contract or global CSS. |
| Homepage featured-category news | Implemented and browser-tested | Section 16 receives `featuredNews` through homepage bootstrap v3 and renders the ten newest unique public articles belonging to active categories marked featured. The additive selected `category_url` drives the category tag without deriving a slug. Thumbnail, title, tag and `Xem thêm` are separate accessible links that open the canonical article/category route in a protected new tab; legacy payloads keep a non-link tag. Geometry, carousel state and server-only loading remain unchanged. Focused Playwright passes 5 with 5 intentional project skips; both app pipelines, 173 unit tests, 25 applied integrations with 12 guarded skips and healthcheck 22/22 pass. |
| Real combo sets / combo cart / combo orders | Code/schema implemented; active catalog unpopulated | Combo API/UI/quote/order behavior exists, but `it_tech_db` has 0 combo sets/relations; 1,121 imported comboset occurrences remain pending audit because source runtime configuration is incomplete |
| Storefront catalog/search/collections | Implemented and locally verified | Bounded APIs, ETag, reduced payloads, cache/single-flight and responsive images. Collection detail presents a visible collection H1 before sanitized DB description HTML, then a gradient-name/solid-count H2 above its 24-item grid; it exposes only canonical low/high price sort links plus Link pagination and uses six/four/three/two columns across `xl`/`lg`/`sm`/mobile. Collection detail and Homepage Sections 8, 10 and 11 share one product-card DOM/style contract; a 260px CSS-container threshold keeps prices, stock state and cart action aligned without overflow. Category headings and SSR titles still fall back from invalid SEO titles to the category name. Category static HTML centers root-level and single-paragraph images at a minimum 60% content width with a 12px corner radius from 1024px without changing product descriptions or smaller viewports. |
| Homepage featured collection carousel | Implemented and browser-tested | Section 8 renders ten sellable products from collection 896 through the existing bootstrap, remains server-rendered, and uses the shared `ProductGridCard` inside 280px desktop / 180px mobile carousel items. It provides matching accessible previous/next arrow controls, canonical product links and cart actions. The homepage-only raw script ported from `index.html` controls every non-hero `.carousel-track` with a one-card buffer, optional clones, three-second auto-slide, mouse/touch threshold gestures, hover pause, indicators and resize recalculation; pointer movement suppresses its synthesized card click, and the Next adapter owns init/destroy cleanup without adding client state. |
| Homepage Sections 6/10/12/13 | Implemented but temporarily unmounted | Homepage no longer imports/renders these four sections and no longer loads categories 178/521 through bootstrap or fallback. Sections 12/13 are static placeholders with no API dependency. Component files, CSS, shared renderer, generic endpoint and carousel controller remain intact. Section 17/category 1087 is the only retained homepage-product-section pipeline. Focused desktop/mobile regression requires surrounding Sections 5/7/9/11/14 to remain ordered with no browser product-section request. |
| Homepage category-feature sections | Implemented, migrated, and browser-tested | Section 11 derives safe category targets and loads up to nine distinct sellable products across enabled descendants. Below 640px it removes the outer frame and presents a 130px compact hero, category/action row and ~1.8-card carousel using the unchanged shared card. The shared homepage controller adds three-second seamless autoplay, 20% touch/mouse drag, offscreen/focus/tab pause and reduced-motion native-scroll fallback; `sm` cleans up the controller and restores the existing grid, while `xl` retains the half-row hero / 3 + 6 layout. Focused Playwright covers exact geometry, autoplay lifecycle, touch threshold, reduced motion, accessibility and live breakpoint cleanup. |
| Performance/UX optimization | Implemented foundation; current JS budgets failing | Core/supplemental streaming, byte-bounded SWR cache, ETag/timing/metrics, same-origin browser APIs, quote debounce, image/motion improvements. Current build: product detail 240.1 KB, cart 174.9 KB, checkout 190.2 KB and combo checkout 186.8 KB exceed regression/release limits; combo cart passes at 167 KB. |
| Durable page views | Implemented and locally verified | Four public page types emit idempotent same-origin events; worker batches into canonical BIGINT totals without hot writes to legacy content rows. Restore-verified migration and local HTTP/browser/integration checks pass; 150-RPS staging gate remains pending. |
| Dynamic breadcrumbs | Implemented and locally verified | Shared semantic component; bounded product/news hierarchy resolver; no extra storefront request or document overflow |
| Product related content | Implemented | Similar products with direct-parent fallback, title-ranked/fallback news, and versioned browser-local recently viewed history. Both product-card sections use six initial / 12 maximum cards and six columns from 1536px. |
| Product/category buying guides | Code/schema implemented; active catalog unpopulated | Independent admin-managed content, detail-only API payload, accessible storefront accordion, and selective cache invalidation exist; `it_tech_db` currently has 0 guides |
| Product groups / independent SKU variants | Code/schema implemented; active catalog unpopulated | Admin CRUD, one-group-per-product index, detail-only payload, thumbnail resolution, carousel, and orphan-safe reads exist. The editor now gives every attribute a desktop two-column section with names/values on the left and its per-SKU value assignment table on the right; current config-group tables have 0 rows and imported variant/config references remain audit-only. |
| Guest cart and checkout | Hardened and locally verified | Strict quote, transactional order, voucher lock, idempotency, outbox, CAPTCHA/rate limit |
| Product voucher discovery | Code/schema implemented; active catalog unpopulated | Resolver/dialog/final quote contracts exist; `it_tech_db` currently has 0 vouchers, so product detail safely hides the section |
| Product promotion display | Implemented and locally verified | Admin CRUD, scope/schedule/priority resolver, and detail rendering exist. Create/edit modals clear the fixed header by 80px; priority is a strict text integer field; optional detail URLs suppress links. Product detail appends sanitized paragraph/line/list-item promotions from legacy `specialOffer` after all managed promotions and continues one number sequence without a new request. `it_tech_db` currently has one enabled managed promotion, applying to product 12767. |
| Product video and specification utilities | Implemented locally | Detail payload safely normalizes legacy YouTube video data, utility cards are data-conditional, and the video/specification dialogs support keyboard close, focus containment, and trigger-focus restoration |
| Customer accounts | Implemented | Registration/OTP/login/reset/profile/address/order flows and admin CRM surfaces exist |
| Customer product favorites | Implemented, migrated, and browser-tested | Additive customer/product relation, authenticated bounded APIs, batched mounted-card status, login continuation, and standalone cursor-paginated `/yeu-thich` page; live table starts empty |
| Admin auth and RBAC | Implemented and local-login recovered | Session, role/permission checks, write gate, audit surfaces, login throttling; development CSP permits React/Turbopack `unsafe-eval` only outside production, local CAPTCHA bypass and a `200` admin login regression check passed |
| Footer Menu | Dynamic, published, and locally verified | Dedicated draft/publish menu `footer`, admin screen/API and public ETag endpoint now accept zero or more named groups with zero or more custom links under one protected root. Empty/inactive groups are omitted and an explicitly empty published menu hides the storefront nav instead of restoring seed content; current published data remains 4 groups / 26 active links. Focused Footer Playwright passes 6/6; both app pipelines, 205 unit tests, 27 applied integrations/13 guarded skips and healthcheck 22/22 pass. |
| Bottom Footer | Dynamic links, published, and locally verified | Dedicated draft/publish menu `bottom_footer` retains one protected group, but its heading and custom-link count are dynamic, including zero links. An explicitly empty published list hides the partner rail instead of restoring the 19-link seed; current published data remains `Trusted Partners` / 19 active links. The same full verification and focused Footer suite pass. |
| Admin content/catalog | Implemented first production-oriented pass | Product/category/article/menu/banner/collection/voucher/customer/order/user/role management; the collection editor uses a responsive 2x2 control grid, a keyboard-accessible searchable parent tree, clear Vietnamese boolean labels, preserves hidden legacy icons, and shares the inset native-select arrow contract with the storefront |
| Admin rich-text editor | Implemented and locally verified | Shared offline GPL TinyMCE keeps its dark theme, hides the promotion CTA/container, and exposes the existing full-width menubar/wrapping toolbar unchanged. Its native `Insert/Edit Image` dialog now includes an image-only file picker across product, category, collection and article editors; uploads are scope/RBAC checked, signature validated and resolved to durable local-media URLs without Base64 content. |
| Article-category featured state | Implemented and migrated | Edit exposes strict `Không`/`Nổi bật` 0/1 choices; the list has an accessible focused toggle backed by an RBAC/write-gated PATCH. Additive metadata was backfilled for all eight categories without changing the legacy import table, and transaction/validation tests pass. |
| Storefront news-category template | Implemented and browser-tested | `/tin-tuc/[category-slug]` uses the reference HTML's bento plus 70/30 layout with real API data, canonical pagination, database-featured sidebar categories, global popular news and a reusable promo banner. Featured categories are isolated in reusable Server Component `FeaturedNewsCategories` with a `NewsCategory[]` prop; the ranking panel is isolated in reusable Server Component `MostReadNews` with a `NewsItem[]` prop. The intermediate category-filter/sort strip is removed; direct sort URLs remain compatible. The promo is sticky at `top: 110px` on desktop and normal-flow on mobile. The page remains server-first, has no database access, and desktop/mobile geometry and edge states pass. |
| Storefront news landing template | Implemented and browser-tested | `/tin-tuc` binds real data into `page-tin-tuc.html`'s 2 + 3 hero, six-item list, normal-flow featured/promotion sidebar, PCM Official playlist and 2 + 4 review grid. `GET /api/news/landing` owns configured-category membership/deduplication and safe YouTube-feed aggregation; the storefront remains database-free and only the playlist interaction hydrates. Missing categories/feed data render explicit empty/error states without sample cards. |
| Storefront news-article template | Implemented and browser-tested | `/tin-tuc/[article-slug]` binds real data into `single-bai-viet.html` with the reference 70/30 layout, sanitized content, real metadata/tags, six newest same-displayed-category articles and an accessible Facebook/X/copy island. The right sidebar directly imports all three reusable panels, omits “Cùng danh mục”, and keeps the promo sticky at `top: 110px` only on desktop. |
| Admin attribute management | Implemented and locally verified | Real list/search/sort/bulk controls and transactional attribute/value/category CRUD; all 426 value ApiKeys are populated, editable, slug-validated and authoritative for public filters; destructive integration test is disposable-database gated and was not run against `it_tech_db` |
| Category attribute filters | Implemented and locally verified | Sidebar metadata and product filtering share one versioned resolver and the stored value ApiKey; unmapped Local values fall back to actual sellable product assignments. Category 1106 returns brand + 9 groups / 39 values, and `cpu=amd-ryzen-7` reduces the 423-product scope to 79 products |
| Search | Implemented and locally verified | Runtime search in `web-admin`, prewarm/single-flight and signed webhook. A shared exact-intent registry applies positive title gates before pagination/facets: existing `PC` remains limited to complete-PC title prefixes; `win 11`/`win11`/`windows 11` return only Windows 11 software; `mic`/`micro` only microphones; `hdd` only HDD; and `loa` only speakers. SSD/HDD synonyms are separated and generic `ổ cứng` stays neutral. The active catalog returns 580 PC, `2/2/2` Windows, `3/3` microphone, 6 HDD and 6 speaker products; `search-tool` is reference only. |
| Runtime topology | Implemented as configuration | Caddy, PM2, readiness/liveness, two API workers, storefront, background worker |
| Database cutover and runtime collation | Applied and accepted on `it_tech_db` | Runtime points to `it_tech_db`; accepted recovery cleanup plus additive article-category metadata/page-view tables leaves zero Latin-1 tables/columns and 292 physical tables; `hanoi23_db` remained untouched |
| PCMarket category import | Applied to `it_tech_db` | Run 2 imported 788 categories and 788 unique routes; 60 roots, depth 3, 722/66 status; run 3 applied all 162 category-attribute links |
| Product category routes/tree scope | Applied and locally verified | 788/788 routes are canonical with zero hash/orphan/duplicate failures; enabled descendant scope is shared by product/count/price/filter APIs; category 521 returns 34 distinct enabled products |
| Product-description disclosure | Implemented and browser-tested | Long descriptions keep their visible bounded preview and expand/collapse with a native accessible control; the article body is never hidden in the initial state |
| Product-description missing-article fallback | Implemented and browser-tested | Products without article HTML reuse the existing disclosure/content structure, show their name without the `Đánh giá:` prefix plus the existing thumbnail and an unmarked normalized `proSummary` list; the collapse-height reference and article path are retained |
| Responsive product technical specifications | Implemented and production-tested | On desktop, a naturally taller specification table clips to the description's collapsed height and keeps the modal action; the independent outer boundary follows the current description height so `top-6` sticky persists until its bottom. A fitting table renders fully without an overlay/action. Mobile retains the existing `66vh` behavior |
| Empty similar-product detail section | Implemented and browser-tested | The entire similar-products region is omitted when the supplemental recommendation list is empty; other product-detail related sections remain independent |
| Product-detail quick specifications | Implemented and browser-tested | Summaries with at most five non-empty rows show in full without a toggle; longer summaries show five rows initially and expand/collapse through an accessible native button |
| Storefront catalog pagination | Implemented and browser-tested | Category/search pagination is URL-driven and SSR-safe; page-two reload/history works, filters reset to page one, and API failures render an inline retry instead of the route error boundary |
| Progressive image hydration | Implemented and production-smoked | Cached success and failure are reconciled after hydration across the shared image component; 16/16 Playwright and category/product/news smoke report zero loaded images stuck blurred |
| Product-card image framing | Implemented and production-tested | Real product cards use a non-shrinking square frame with centered `object-fit: contain`; portrait/landscape/square assets and all supported card surfaces pass desktop/mobile and four-breakpoint checks |
| PCMarket product import | Applied to `it_tech_db` | Run 3 imported 4,712 products/search rows, 14,455 category links, 17,603 attribute links, 91 brands, 45 attributes, and 426 values; images remain remote HTTPS PCMarket URLs; incomplete variant/config/combo data remains audit-only pending |
| PCMarket brand sync | Run 8 applied and accepted on `it_tech_db` | Public PCM policy maps `0 -> 96`; E-DRA/TEAMGROUP keep `34 -> 25` and `57 -> 31`; runtime has 90 brand/info rows, 1,587 brand-category rows, PCM 2,276/849 products, and zero noncanonical references |
| PCMarket article-category import | Applied to `it_tech_db` | Run 6 imported 4 enabled UTF-8 root categories with source IDs, `.html` routes, registry/map/record audit rows, and no article/menu writes; clone apply/verify/rollback and live smoke passed |
| PCMarket article import | Applied to `it_tech_db` | Run 7 imported 668 articles/content rows, 705 unique category links and 668 routes/registry/maps; ID 83 is quarantined and no menu was created |
| Database transfer | Fresh pre-v4 backup restore-verified; post-v4 export still required for machine transfer | The rollback artifact has 303 tables/97,581 rows and SHA-256 `657d673b...`; current accepted schema has 301 tables after removing verification data. Create a fresh post-v4 export before moving machines; destination-version compatibility still requires target testing. |
| Functional verification | Required compile/test/build matrix and controlled browser suite pass; performance gates remain pending | Both typecheck/lint/build pipelines pass; 173/173 unit tests and default integration 25 pass/12 fixture- or safety-gated skips. Search ranking traverses every strict-intent result under default/price/newest; browser smoke renders only valid cards for all seven Windows/microphone/HDD/speaker aliases, and local healthcheck passes 22/22. The last full four-worker browser suite remains 107 pass/19 expected skips across 126 cases; production-like load and JavaScript budgets remain separate release gates. |
| 1,500-VU capacity | Not yet verified | Full k6 production-like run remains a release blocker |

## Completed implementation highlights

- Replaced the rejected Section 8-specific React carousel with the raw homepage algorithm from `font-end/index.html`. Section 8 now renders only its server-provided collection markup, while one homepage script controls every non-hero `.carousel-track` through the reference one-card buffer, 400 ms transform/reset, three-second timer, mouse/touch gestures, hover pause, indicators and resize behavior. A lifecycle adapter removes clones, inline state, timers and listeners before Next.js remounts.
- Added an opt-in shared-card variant to the existing Section 6/10/17 homepage product renderer and enabled it only for Section 10. Category data, CTA markup, SSR/bootstrap flow and carousel mechanics stay unchanged while the product presentation now follows Section 11.
- Rebuilt `/collection/[slug]` as a server-rendered collection surface: safe database-authored description markup appears first without typography overrides; sort and pagination are canonical URL links; the fixed 24-product grid renders six/four/three/two columns without horizontal card overflow; empty descriptions, empty collections and one-page collections omit their unused regions.
- Backfilled all 426 legacy attribute values with collision-free canonical ApiKeys, made the admin field required/editable, and changed category/search payloads, URL state and backend matching to use the stored key instead of rebuilding a slug from the display name.
- Added customer product favorites end to end: an InnoDB relation with customer cascade and logical product cleanup, session-only list/status/idempotent mutation APIs, mounted-card status batching, optimistic synchronized hearts, login-save continuation, a noindex `/yeu-thich` grid with retry/load-more/remove, and header entry points. A restore-verified pre-migration backup and disposable clone trial preceded the live migration.
- Repaired product-description disclosure so its preview is visible while collapsed; a focused client wrapper owns only the expand state and preserves the server-rendered sanitized article body.
- Added a server-rendered no-article product-description fallback: it reuses the thumbnail and normalized summary lines in semantic but visually unmarked list markup, while retaining the normal article disclosure whenever article HTML exists.
- Added a minimal client-only height controller around server-rendered technical specifications. It batches live DOM measurements, handles streamed node replacement and responsive changes, retries transient zero-size startup measurements, keeps the visible preview tied to the collapsed description height, and drives an independent current-height sticky boundary plus CSS-only `pending`/`clipped`/`full` states without serializing the specification HTML.
- Removed the product-detail similar-products empty state: no recommendation cards now means no heading, card frame, grid, or section semantics in the rendered page.
- Replaced the broken closed `<details>` summary with a five-row preview and native ARIA disclosure; all rows remain in the DOM, hidden overflow is removed from the accessibility tree, and product changes reset the preview to collapsed.
- Replaced the mount-time progressive-image reset with source-bound state plus cached DOM reconciliation, preserving callbacks/fallbacks while preventing cached hard refreshes from remaining at `blur-sm`/`blur-md` loading styles.
- Standardized real product-card media on one CSS-only square/contain contract and aligned its loading skeleton; `flex-shrink: 0` preserves the square inside constrained legacy mobile cards.
- Replaced invalid client-side `new URL('/api/...')` construction in category/search pagination with same-origin rewritten requests, canonical `?page=N` state, bounded SSR page parsing, request cancellation, local retry UX, and accessible pagination controls.
- Repaired all 788 live product-category routes from legacy type `0` to `product:category` with exact preimage hash `7ad32ed2cbdc7ccc99b3f89703d637c8592a28b8209b21074eec70cc6ada18c7`; no route hash, orphan, or duplicate changed.
- Added exact-prefix catalog resolution, canonical route writes in the importer/admin, read-only applied-route assertions, guarded external-artifact repair/rollback, shared enabled-descendant scope, category cache invalidation, and the measured `(parentId,status,id)` index.
- Category 521 now resolves through 29 enabled nodes to 34 distinct enabled products across pagination, price bounds, brand and attribute filters. Its storefront and a child route render on port 3001; disabled category 71 stays 404 and product/news detail stays 200.
- Restore-verified pre/post cutover backups (`e47e5232...` / `4d7c5249...`) preserve the 288-table, 84,049-row database. Source verification still matches categories/products/brands/article categories; article source has two new, unimported records (682/683) under hash `aaeda512...`.
- Added public PCM fallback policy (`0 -> 96`), future source-ID collision protection, durable audit mapping, read-only `--verify-applied`, rollback-window closure, and guarded exact-name recovery cleanup.
- Clone trial applied run 8, verified all relations/API counts, rolled back to the exact baseline, applied again, added measured news indexes, dropped 88 clone recovery tables, and passed healthcheck on the 288-table lean schema.
- Live run 8 applied the stable brand hash, added four news indexes, closed runs 2-8 after a restore-verified 362-table backup, and removed exactly 74 recovery tables. Final live state is 288 tables (160 InnoDB/128 MyISAM), zero Latin-1/recovery/stage/restore objects, and `ADMIN_WRITE_ENABLED=false`.
- Replaced news category `OR` membership/dependent subqueries with `UNION DISTINCT`; EXPLAIN selects the status/date, URL/status, and both covering junction indexes.

- Added guarded `pcmarket/article-categories` dry-run/apply/rollback with strict Zod validation, bounded HTTPS-only double snapshots, canonical SHA-256 locking, source-tree/route/media validation, advisory locking, exact empty-target guards, and retained run-scoped backups.
- Restored the fresh pre-import dump into `it_tech_db_article_category_test_20260713_203630`; clone run 6 applied and verified all four categories/routes/audit relations, then rollback restored the exact empty state while retaining the imported snapshot.
- Applied live run `6` with snapshot `0a3d22d053ec9feb5f6eadf752b4191a240b5e0010515f671a84fd0a34204b04`. The database now has 4 article categories, 4 canonical routes, 4 registry/map/record entries, 0 articles/links/menu references, 346 tables, and 152,162 exact rows; catalog/search/full-text/routine/trigger counts are unchanged.
- Restarted web-admin, storefront, and worker with persisted `ADMIN_WRITE_ENABLED=false`; all four `/api/news-category/<slug>` and `/tin-tuc/<slug>` paths return 200 with correct UTF-8 names and `totalNews=0`, and the healthcheck passed 15/15.
- Added guarded `pcmarket/articles` dry-run/apply/rollback with strict two-pass snapshots, ID 83 quarantine, duplicate-category removal, HTML/HTTPS normalization, MyISAM staging/swap, InnoDB compensation and exact run backups.
- Applied live run `7` with hash `0ef9d19c682182113ce43d70b9cb6eb21045a0fb7041287a288716c78b1fab13`: 668 article/content rows, 705 unique links, 668 routes/registry/maps and 669 audit records. Catalog/search/full-text/routine/trigger invariants remained unchanged.
- Replaced demo news UI with active-only paginated APIs/pages, absolute PCMarket images, true 404s, local canonical/Open Graph metadata, bounded related news and defense-in-depth HTML sanitization.

- Added dynamic product/news category trails to existing API payloads, fixed the legacy news-category join, and replaced four breadcrumb implementations with one accessible responsive component.
- Split the product-detail related-content placeholder into similar, recently viewed, and related-post sections; added bounded recommendation and batch-card contracts without schema changes.
- Replaced hardcoded Why Buy content with independent product/category buying guides, a reusable admin editor, bounded transactional APIs, detail-only reads, and selective catalog-detail cache invalidation.
- Added canonical Zod validation and bounded parsing for high-risk commerce/customer routes.
- Added request IDs, safe public error envelopes, origin allowlist, `Retry-After`, atomic rate limits, honeypots, and action-specific reCAPTCHA.
- Reworked order creation around one quote/transaction, bulk item insert, voucher locking, idempotency replay, customer metrics, and email outbox.
- Added Argon2id password writes with bcrypt compatibility/upgrade and improved sliding session expiry.
- Added HMAC/timestamp/nonce protection for search webhook and image-content signature validation for uploads.
- Added DB-backed cross-worker cache versions, ETags, bounded filters/keys, search prewarm, and reduced public payloads.
- Upgraded storefront to Next.js 16.2.9/React 19.2.4 and added a committed ESLint configuration.
- Added Caddy/PM2 topology, background job runner, readiness/liveness, unit/integration tests, benchmark script, and k6 scenario.
- Cut over to the empty `it_tech_db`, copied only approved safe configuration, then imported PCMarket categories, products, attributes, search data, and canonical brands through guarded, auditable, rollback-capable workflows.
- Added canonical brand APIs/pages/homepage data and kept all imported images as validated absolute PCMarket HTTPS URLs.
- Generated a portable full SQL archive, verified its checksum, restored it into a disposable database, compared schema/counts, and documented CLI/phpMyAdmin transfer procedures.

## Latest verification matrix

| Check | Result |
|---|---|
| `web-admin` TypeScript | Pass |
| `font-end` TypeScript | Pass |
| `web-admin` ESLint `--quiet` | Pass |
| `font-end` ESLint `--quiet` | Pass |
| `web-admin` production build | Pass |
| `font-end` production build | Pass |
| Web-admin unit tests | 144/144 pass, including strict PC intent/exclusion/synonym regressions, product-editor promotion splitting/sanitization/ordering, canonical page-view validation, rich-text uploads, optional promotion links and strict priority validation |
| Admin local login regression | Pass: CSP development header includes `unsafe-eval`, local CAPTCHA bypass accepts the empty development token, and `POST /api/admin/auth/login` returned `200` |
| Default DB integration suite | 17 pass, 7 environment- or safety-gated skips; public product detail verifies managed-before-editor promotion projection without exposing raw HTML; destructive CRUD/import tests remain correctly gated |
| Destructive importer integration | Category, product, and brand apply/rollback passed independently on disposable databases, then the databases were dropped |
| npm audit in both apps | 0 known vulnerabilities |
| Local healthcheck | Current strict rerun: 13/15; both configured legacy collection probes return 404. Current documented empty-catalog mode passes 15/15. |
| Full database restore | Final lean bundle pass: 288 tables, 84,040 rows, 1 routine, 2 triggers; 788 categories, 90 brands, 4,712 products/search rows and 668 articles/content rows |
| Liveness/readiness/storefront | Initially HTTP 200; MySQL was no longer listening at audit end, so readiness then returned 503 and requires a runtime rerun after database restart |
| Invalid quote/origin/order-key/webhook probes | Expected safe 4xx/5xx responses |
| Full k6 1,500 VU | Not run on production-like host |
| Playwright + axe | Last full controlled four-worker run: 107 pass/19 expected project/device/data skips across 126 desktop/mobile cases. The focused product-promotion regression adds 2/2 passing desktop/mobile checks for managed-before-editor order, numbering, formatting, overflow and Axe. |
| Lighthouse CI | Configuration added; local Windows Chrome run was inconclusive because the temporary profile cleanup failed with `EPERM`, so staging artifacts remain required |
| Regression bundle budget | Fail: product 236.8 KB, cart 175.5 KB, checkout 190.8 KB, combo-checkout 187.4 KB; combo-cart passes at 167.7 KB |
| Strict release bundle budget | Same four routes fail their stricter limits; combo-cart passes 167.7 KB <170 KB |

## Flash Sale implementation (2026-07-19)

- Implemented additive InnoDB campaign, SKU quota, allocation ledger and HMAC buyer-usage contracts without changing legacy catalog/order tables.
- Added guarded/idempotent migration verification, admin list/editor/publish/pause workflows, `marketing.flash_sales` RBAC/audit, and the public active/upcoming feed.
- Integrated Flash price and quota into product projection, cart quote and the existing order transaction. Order completion consumes reserved quota; cancellation/failure releases it; exclusive campaigns reject voucher stacking.
- Added the dark responsive `/flash-sale` storefront, countdown, scheduled campaign tabs, colored semantic remaining-quota bars, sold-out controls and global product-card indicators.
- A fresh 301-table/95,625-row logical backup was restore-verified before rollout (SHA-256 `72063f78d1562e078ff71822f3006c80ee3155dba2afecf4c1cc3a0f03d04223`). Guarded Flash Sale apply passed twice on `it_tech_db`, then verify passed. The database now has 305 tables (177 InnoDB/128 MyISAM), including four empty Flash Sale tables with zero invariant violations; runtime is enabled.
- Verification passes both application TypeScript, quiet ESLint and production builds; backend unit tests pass 160/160; integration tests pass 20 with 10 expected safety/fixture skips; focused desktop/mobile Flash Sale Playwright passes 2/2. Expanded local healthcheck passes 22/22, including public API, admin page and storefront Flash Sale probes. Post-migration admin list and public enabled/empty response smoke pass. Production-like load and a real low-quota campaign checkout lifecycle remain release evidence gates.

## Latest local performance observations

| Measurement | Observed |
|---|---:|
| Products cold / warm | ~28.1 ms / ~3.1 ms |
| Search cold / warm | ~126.1 ms / ~2.0 ms |
| Homepage bootstrap cold / warm | ~21.6 ms / ~4.8 ms |
| Header payload | 51,415 bytes, down from 99,097 |
| Homepage bootstrap payload | 96,610 bytes, down from 148,256 |

These results are development-machine observations and must not be used as production SLO evidence.

## Prioritized next work

1. Preserve/transfer the dirty working tree plus ignored database/media/secrets using `NEW_MACHINE_SETUP.md`; restore-verify the destination.
2. Keep the restored local MySQL readiness and runtime/database assertions monitored.
3. Fix the current frontend JS-budget regressions; keep future full Playwright runs at the verified controlled concurrency.
4. Import missing variant/config-group/comboset definitions only after complete source exports are available and reconcile the pending audit records.
5. Run the complete read/commerce/abuse k6 scenarios on an isolated target-like host and capture application, MySQL, CPU, RAM, pool, and slow-query metrics.
6. Validate production reCAPTCHA hostnames/actions/scores in shadow mode, then enable enforcement in a coordinated frontend/backend release.
7. Exercise destination-version database restore, graceful PM2/Caddy restart, worker crash recovery, and outbox retry/backoff against staging.
8. Add integration/E2E coverage for remaining write-route groups/forms and audit legacy admin parity, canonical schemas, RBAC, accessibility, and error UX.

## Storefront validation status (2026-07-14)

- Added shared client validators and structured API errors retaining HTTP status, code, field paths, retry delay, and request ID.
- Registration and customer/commerce forms now report specific field, CAPTCHA, network, and server errors with accessible associations; checkout receiver/invoice/delivery validation is canonical on the backend.
- Quantity/voucher/search/price controls are bounded before navigation or quote calls. Newsletter and comments are explicitly unavailable instead of presenting fake submissions.
- No database schema or data changed. Production reCAPTCHA/SMTP environment verification remains a release gate.

## Known risks and blockers

- Capacity is not certified until the full k6 release gate passes.
- CAPTCHA enforcement depends on production site/secret keys and allowed hostnames; a missing or mismatched configuration can block real users.
- Search webhook updates depend on a shared strong `SEARCH_WEBHOOK_SECRET`.
- MySQL shares the target host with the applications. If CPU/RAM/pool/SLO targets fail, separate MySQL or add another host.
- The database still mixes InnoDB and MyISAM; transaction behavior cannot be assumed across all legacy tables even though character data is now fully UTF-8.
- Runs 2-8 no longer have in-database rollback tables. Recovery now depends on the verified external backup artifacts, which must be protected and periodically restore-tested.
- PCMarket variant/config/comboset references are audit-only pending data, not complete sellable runtime configuration.
- Shared validation/security foundations cover the highest-risk routes, but the project should not claim every legacy admin field has been converted to canonical Zod without a route-by-route follow-up audit.
- Current frontend regression/release JS budgets are red on four routes.
- Full Playwright is green at four workers; higher-concurrency local runs can still exhaust Windows resources and should not be treated as product failures without reproduction.

## Verification commands

Use the command blocks in `AGENTS.md`. Use `npm.cmd run local:healthcheck` and `npm.cmd run local:benchmark` from `web-admin` while both apps are running. Use `npm.cmd run load:k6` only against an approved isolated target.
