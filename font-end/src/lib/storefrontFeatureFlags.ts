/**
 * Temporary presentation switch for the favorite control on catalog cards.
 * Set this back to `true` to restore the existing card UI without changing
 * the favorite API, context, or button implementation.
 */
export const SHOW_PRODUCT_CARD_FAVORITES = false;
